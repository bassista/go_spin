services:
  api:
    container_name: gospin
    image: gospin
    environment:
      - PORT=${PORT:-8083}
    ports:
      - "${PORT:-8083}:${PORT:-8083}"
    volumes:
      - ./:/app
    build:
      dockerfile: dev.Dockerfile
    deploy:
     resources:
      limits:
        cpus: '2'
